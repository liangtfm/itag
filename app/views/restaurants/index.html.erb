<%= link_to "Back", cities_url %>
<h1>All Restaurants in <%= @city.name %>, <%= @city.state.name %></h1>
<%= link_to "Add a new restaurant", new_restaurant_url %>

<ul>
<% @restaurants.each do |restaurant| %>

  <li><%= image_tag restaurant.photo.url(:small) %> <%= link_to restaurant.name, restaurant_url(restaurant) %> (Average Rating: <%= restaurant.average_rating %>)</li>

<% end %>
</ul>